package ru.komiss77.scoreboard;
/*
import java.util.Collection;

import org.bukkit.Bukkit;
import org.bukkit.entity.Player;
import org.bukkit.event.Listener;
import ru.komiss77.modules.player.Oplayer;
import ru.komiss77.version.VM;




public class NametagManager implements Listener {
*/
   // public NametagManager(Ostrov plugin) {
        //Bukkit.getPluginManager().registerEvents(this, plugin);
   // }

    
    

    //@EventHandler (priority = EventPriority.HIGHEST, ignoreCancelled = false)
   // public void onPlayerJoin(final PlayerJoinEvent e) {
    //	for (final Player p : Bukkit.getOnlinePlayers()) {
  //  		updateTag(PM.getOplayer(p.getName()), p, e.getPlayer());
   // 	}
   // }


    /*@EventHandler (priority = EventPriority.MONITOR, ignoreCancelled = false)
    public void onQuit(final PlayerQuitEvent e) {
        VM.getNmsNameTag().resetNametag(e.getPlayer());
    }    
    
    

        public static void updateTag(final Oplayer op, final Player of) {
        updateTag(op, of, Bukkit.getOnlinePlayers());
    }

    public static void updateTag(final Oplayer op, final Player of, final Player to) {
        VM.getNmsNameTag().updateTag(op, of, to);
    }

    public static void updateTag(final Oplayer op, final Player of, final Collection<? extends Player> to) {
        VM.getNmsNameTag().updateTag(op, of, to);
    }

    


    public void setNametag(final Player p, String prefix, String suffix) {
        VM.getNmsNameTag().setNametag(p, prefix, suffix);
    }
    
    public void reset(final Player p) {
         VM.getNmsNameTag().resetNametag(p);
    }*/

/*
    public static void updateTag(final Oplayer op, final Player of) {
        updateTag(op, of, Bukkit.getOnlinePlayers());
    }

    public static void updateTag(final Oplayer op, final Player of, final Player to) {
        VM.getNmsNameTag().updateTag(op, of, to);
    }

    public static void updateTag(final Oplayer op, final Player of, final Collection<? extends Player> to) {
        VM.getNmsNameTag().updateTag(op, of, to);
    }



}
*/